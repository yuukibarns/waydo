[Unit]
Description=Waydo pie menu daemon
After=graphical-session.target
Wants=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.cargo/bin/waydo daemon
Restart=on-failure
RestartSec=1
# Optional hardening:
NoNewPrivileges=true

[Install]
WantedBy=default.target
